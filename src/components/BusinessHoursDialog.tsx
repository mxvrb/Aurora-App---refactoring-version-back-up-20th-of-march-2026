// This component has been replaced by the direct Dialog implementation in App.tsx
// The separate component was causing duplicate X buttons
// Keeping this file empty to prevent import errors, but functionality moved to App.tsx

export function BusinessHoursDialog() {
  return null;
}